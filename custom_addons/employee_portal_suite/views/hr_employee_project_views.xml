<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="hr_employee_form_material_projects" model="ir.ui.view">
        <field name="name">hr.employee.form.material.projects</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='work_location_id']" position="after">
                <field name="available_work_location_project_ids" invisible="1"/>
                <field name="material_project_ids"
                       widget="many2many_tags"
                       options="{'no_create': True, 'no_create_edit': True}"
                       placeholder="Select this employee's projects"/>
            </xpath>
        </field>
    </record>
</odoo>
