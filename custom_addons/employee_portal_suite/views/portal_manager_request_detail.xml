<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <template id="portal_manager_request_detail" name="Manager Approval Detail">
        <t t-call="employee_portal_suite.employee_portal_layout">

            <div class="container mt-3 employee-req-detail">

                <!-- Back -->
                <div class="mb-4">
                    <a href="/my/employee/approvals" class="app-back-btn">
                        <i class="fa fa-arrow-left"></i>
                        <span>Back</span>
                    </a>
                </div>

                <!-- HEADER -->
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <div>
                        <h4 class="mb-0 fw-bold">Employee Request</h4>
                        <div class="text-muted small">
                            <t t-esc="request_rec.name"/>
                        </div>
                    </div>

                    <div>
                        <t t-raw="status_badge(request_rec)"/>
                    </div>
                </div>

                <!-- PDF -->
                <t t-if="request_rec.state in ['approved','rejected']">
                    <div class="mb-3">
                        <a t-att-href="'/report/pdf/employee_portal_suite.report_employee_request_document/%s' % request_rec.id"
                        class="btn btn-outline-primary btn-sm"
                        target="_blank">
                            Download PDF
                        </a>
                    </div>
                </t>

                <!-- REJECT ALERT -->
                <t t-if="request_rec.state == 'rejected'">
                    <div class="alert alert-danger small">
                        <strong>Rejected at:</strong>
                        <t t-esc="request_rec.state_before_reject"/> —
                        <strong>Reason:</strong>
                        <t t-esc="request_rec.get_rejection_reason()"/>
                    </div>
                </t>

                <!-- ========================= -->
                <!-- OVERVIEW CARD -->
                <!-- ========================= -->

                <div class="card mb-3 rounded-4 shadow-sm">
                    <div class="card-body small">

                        <div class="row g-3">

                            <div class="col-6">
                                <div class="text-muted">Employee</div>
                                <div class="fw-semibold">
                                    <t t-esc="request_rec.employee_id.name"/>
                                </div>
                            </div>

                            <div class="col-6">
                                <div class="text-muted">Type</div>
                                <div class="fw-semibold">
                                    <t t-esc="request_rec.request_type"/>
                                </div>
                            </div>

                            <div class="col-6">
                                <div class="text-muted">Request Date</div>
                                <div class="fw-semibold">
                                    <t t-esc="request_rec.request_date"/>
                                </div>
                            </div>

                            <t t-if="request_rec.request_type == 'leave'">
                                <div class="col-6">
                                    <div class="text-muted">Leave From</div>
                                    <div class="fw-semibold">
                                        <t t-esc="request_rec.leave_from"/>
                                    </div>
                                </div>

                                <div class="col-6">
                                    <div class="text-muted">Leave To</div>
                                    <div class="fw-semibold">
                                        <t t-esc="request_rec.leave_to"/>
                                    </div>
                                </div>
                            </t>

                            <div class="col-12">
                                <div class="text-muted">Description</div>
                                <div class="fw-semibold">
                                    <t t-esc="request_rec.description"/>
                                </div>
                            </div>

                        </div>

                    </div>
                </div>

                <!-- ========================= -->
                <!-- APPROVAL ACTION -->
                <!-- ========================= -->

                <t t-if="request_rec.state not in ['approved','rejected']">

                    <div class="card mb-3 rounded-4 shadow-sm">
                        <div class="card-header bg-white fw-semibold">
                            Approval Action
                        </div>

                        <div class="card-body small">

                            <!-- Approve -->
                            <form action="/my/employee/requests/approve"
                                method="post"
                                class="mb-4">

                                <input type="hidden" name="csrf_token"
                                    t-att-value="request.csrf_token()"/>
                                <input type="hidden" name="req_id"
                                    t-att-value="request_rec.id"/>

                                <label class="form-label fw-semibold small">
                                    Approval Comment (optional)
                                </label>

                                <textarea name="comment"
                                        class="form-control mb-3"
                                        placeholder="Add comment..."></textarea>

                                <div class="d-flex justify-content-end">
                                    <button type="submit"
                                            class="btn btn-success px-4">
                                        Approve
                                    </button>
                                </div>
                            </form>

                            <hr/>

                            <!-- Reject -->
                            <form action="/my/employee/requests/reject"
                                method="post">

                                <input type="hidden" name="csrf_token"
                                    t-att-value="request.csrf_token()"/>
                                <input type="hidden" name="req_id"
                                    t-att-value="request_rec.id"/>

                                <label class="form-label fw-semibold small text-danger">
                                    Rejection Reason (required)
                                </label>

                                <textarea name="comment"
                                        class="form-control mb-3"
                                        placeholder="Explain why you are rejecting..."
                                        required="required"></textarea>

                                <div class="d-flex justify-content-end">
                                    <button type="submit"
                                            class="btn btn-outline-danger px-4">
                                        Reject
                                    </button>
                                </div>
                            </form>

                        </div>
                    </div>

                </t>

            </div>

                <!-- ========================= -->
                <!-- TIMELINE -->
                <!-- ========================= -->

                <div class="card mb-3 rounded-4 shadow-sm">
                    <div class="card-header bg-white fw-semibold">
                        Approval Timeline
                    </div>

                    <div class="card-body small">

                        <t t-if="request_rec.get_portal_timeline()">
                            <ul class="timeline">
                                <t t-foreach="request_rec.get_portal_timeline()" t-as="ev">
                                    <li class="timeline-item">
                                        <div class="timeline-marker"></div>
                                        <div class="timeline-content">
                                            <div class="fw-semibold">
                                                <t t-esc="ev['stage']"/>
                                            </div>

                                            <div class="text-muted small">
                                                <t t-esc="ev['date']"/>
                                            </div>

                                            <t t-if="ev['comment']">
                                                <div class="small mt-1">
                                                    <t t-esc="ev['comment']"/>
                                                </div>
                                            </t>
                                        </div>
                                    </li>
                                </t>
                            </ul>
                        </t>

                        <t t-if="not request_rec.get_portal_timeline()">
                            <div class="text-muted small">
                                No approval activity yet.
                            </div>
                        </t>

                    </div>
                </div>



            <!-- ========================= -->
            <!-- TIMELINE CSS              -->
            <!-- ========================= -->
            <style>
                .timeline {
                    list-style: none;
                    padding-left: 20px;
                    position: relative;
                    margin-left: 10px;
                }

                .timeline:before {
                    content: "";
                    position: absolute;
                    top: 0;
                    bottom: 0;
                    width: 2px;
                    background: #c5c5c5;
                    left: 8px;
                }

                .timeline-item {
                    position: relative;
                    margin-bottom: 20px;
                }

                .timeline-marker {
                    position: absolute;
                    width: 16px;
                    height: 16px;
                    background: #0d6efd;
                    border-radius: 50%;
                    left: 0px;
                    top: 5px;
                }

                .timeline-content {
                    margin-left: 30px;
                    padding: 10px 15px;
                    background: #f8f9fa;
                    border-radius: 5px;
                    border: 1px solid #dee2e6;
                }

                .timeline-title {
                    margin: 0 0 5px 0;
                    font-size: 16px;
                    font-weight: 600;
                }
            </style>

        </t>
    </template>

</odoo>
