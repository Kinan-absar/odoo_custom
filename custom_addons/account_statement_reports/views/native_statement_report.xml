<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!--
        Dedicated Account Statement report implemented as an Odoo 18
        account.report variant of Partner Ledger.

        Keep this record deliberately minimal: report filter fields such as
        filter_partner, filter_account_type, filter_show_draft, etc. are
        computed/inherited from root_report_id by Odoo's account.report
        engine. Re-declaring them here made the previous build fragile and
        used an invalid selection value for filter_account_type.
    -->
    <record id="account_statement_native_report" model="account.report">
        <field name="name">Account Statement Reports</field>
        <field name="root_report_id" ref="account_reports.partner_ledger_report"/>
    </record>

    <record id="action_account_statement_native" model="ir.actions.client">
        <field name="name">Account Statement Reports</field>
        <field name="tag">account_report</field>
        <field name="context" eval="{'report_id': ref('account_statement_native_report')}"/>
    </record>

    <!-- Reuse the existing menu XML id so upgrades do not leave duplicates. -->
    <record id="menu_customer_statement_wizard" model="ir.ui.menu">
        <field name="name">Account Statement Reports</field>
        <field name="parent_id" ref="account.account_reports_partners_reports_menu"/>
        <field name="action" ref="action_account_statement_native"/>
        <field name="sequence">50</field>
        <field name="active">True</field>
    </record>

    <!-- Hide the old separate Vendor Statement menu. -->
    <record id="menu_vendor_statement_wizard" model="ir.ui.menu">
        <field name="active">False</field>
    </record>
</odoo>
