<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- Add "Amount Paid" column to the Purchase Order list view -->
    <record id="view_purchase_order_tree_amount_paid" model="ir.ui.view">
        <field name="name">purchase.order.list.amount.paid</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_tree"/>
        <field name="arch" type="xml">
            <field name="amount_total" position="after">
                <field name="amount_paid" widget="monetary" optional="show"/>
            </field>
        </field>
    </record>

    <!-- Add "Amount Paid" field and a smart button to the Purchase Order form view -->
    <record id="view_purchase_order_form_amount_paid" model="ir.ui.view">
        <field name="name">purchase.order.form.amount.paid</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">

            <!-- Smart button showing how much was paid -->
            <div name="button_box" position="inside">
                <button name="action_view_payment_vouchers"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-money"
                        invisible="payment_voucher_count == 0">
                    <field name="amount_paid" widget="monetary" class="o_stat_value"/>
                    <span class="o_stat_text">Paid</span>
                </button>
            </div>

            <!-- Dedicated tab: Payments, showing totals and the linked vouchers -->
            <xpath expr="//notebook" position="inside">
                <page string="Payments" name="payments">
                    <group>
                        <group string="Payment Summary">
                            <field name="amount_paid" widget="monetary"/>
                            <field name="amount_paid_residual" widget="monetary"/>
                        </group>
                    </group>
                    <field name="payment_voucher_ids" nolabel="1">
                        <list>
                            <field name="name"/>
                            <field name="date"/>
                            <field name="partner_id"/>
                            <field name="amount" widget="monetary"/>
                            <field name="payment_method"/>
                            <field name="state" widget="badge" decoration-success="state == 'posted'" decoration-info="state == 'draft'" decoration-danger="state == 'cancel'"/>
                        </list>
                    </field>
                </page>
            </xpath>

        </field>
    </record>

</odoo>
